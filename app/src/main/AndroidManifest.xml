<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permiso para leer archivos de audio (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Permiso para versiones antiguas de Android (Opcional, pero buena práctica) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Permiso para que Retrofit se conecte a la API -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Característica del sensor de proximidad -->
    <uses-feature android:name="android.hardware.sensor.proximity" android:required="true" />

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IntegradoraSensorProximidad">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.IntegradoraSensorProximidad">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- CORRECCIÓN: La etiqueta de atribución se mueve aquí dentro -->
            <property
                android:name="android.app.PROPERTY_ATTRIBUTION_TAGS"
                android:value="player.features" />
        </activity>
    </application>

</manifest>